\ProvidesPackage{resume-styles}

\NeedsTeXFormat{LaTeX2e}

\usepackage{titlesec}
% \usepackage{fontspec}
\usepackage{color}
\usepackage[left=0.5in, right=0.5in, top=0.4in, bottom=0.4in]{geometry}
\usepackage{graphicx}
% \usepackage{enumitem}
\usepackage{enumitem}
\usepackage{amsfonts}
\usepackage{xhfill}
\usepackage{amsmath}
\usepackage{multicol}
\usepackage{textcomp}
\usepackage[hidelinks]{hyperref}
\usepackage{colors}


% \setmainfont{Arial}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\setlength\parindent{0pt}
% \setlength\hangindent{2em}
% \leftskip=1em

\setlist[itemize]{leftmargin=*}
% \setlist[itemize]{leftmargin=1em}
% \setlength{\itemindent}{-.5in}

\definecolor{c_colrule}{RGB}{100, 100, 100}

\def\labelitemi{~~~\color{c_cyan}\textbullet}

\def\mybullet{{\color{c_cyan}\textbullet}~~}

\pagenumbering{gobble}

\renewcommand{\columnseprulecolor}{\color{c_colrule}}

\newif\ifpaper

\newcommand{\link}[2]{\href{#2}{\underline{#1}}}

\newcommand{\nopaper}[2]{\ifpaper #2 \else #1 \fi}

\newcommand{\sep}{{\color{c_dark_blue}|} }

\newcommand{\headline}[1]{
  {\Large\scshape{\textbf{#1}}}
  \leavevmode
}

\newcommand{\address}[3]{{\textcolor{c_grey}{#1} {\textperiodcentered} \textcolor{c_grey}{#2} {\textperiodcentered} \textcolor{c_grey}{#3}}
}
% \newcommand{\address}[3]{
%   {\textcolor{c_grey}{#1} | \textcolor{c_grey}{#2} | \textcolor{c_grey}{#3}}
% }

\titlespacing*{\section} % \titlespacing{command}{left spacing}{before spacing}{after spacing}[right]
{0pt}{0pt}{4pt}
\titlespacing*{\subsection}
{0pt}{0pt}{3pt}

\titleformat{\section}          % Customise the \section command
{\centering\Large\scshape} % Make the \section headers large (\Large),
% small capitals (\scshape) and left aligned (\raggedright)
% {\textcolor{c_cyan}{| }}                      % Can be used to give a prefix to all sections, like 'Section ...'
{}                      % Can be used to give a prefix to all sections, like 'Section ...'
{0em} % sep
% {}                           % Can be used to insert code before the heading
% [\titlerule]                 % Inserts a horizontal line after the heading
{}                           % Can be used to insert code before the heading
[]                 % Inserts a horizontal line after the heading

\def\mysection#1{{\large\hbox to \hsize{\hrulefill\quad\lower.3em\hbox{{\color{c_dark_blue}\textbf{#1}}}\quad\hrulefill}}}
% \def\mysection#1{\par\noindent\raisebox{.8ex}{\makebox[\linewidth]{\hrulefill\hspace{1ex}\raisebox{-.8ex}{#1}\hspace{1ex}\hrulefill}}\medskip}

\titleformat{\subsection}           % Customise the \subsection command
{\bf\normalsize} % Make the \section headers large (\Large),
% small capitals (\scshape) and left aligned (\raggedright)
{{\color{c_dark_blue}- }}{0em}                      % Can be used to give a prefix to all sections, like 'Section ...'
{}                           % Can be used to insert code before the heading
[]                 % Inserts a horizontal line after the heading

\newcommand{\datedsection}[2]{%
  \section[#1]{#1 \hfill \textcolor{c_dark_blue}{#2}}%
}

\newcommand{\datedsubsection}[2]{%
  {#1 \hfill #2}%
}

\newcommand{\inlineimage}[1]{
  \raisebox{-0.25em}{\includegraphics[height=10pt]{#1}}
% $
% \begin{array}{l}
% \includegraphics[height=10pt]{#1}
% \end{array}
% $
}

\newcommand{\dateditem}[2]{%
  {#1 \hfill #2}%
}

\newcommand{\techitem}[2]{%
  {\color{c_dark_blue} #1 \hfill \textbf{#2}}%
}

\newcommand{\dtext}[1]{
  {\footnotesize\textcolor{c_grey}{#1}}
}

\newcommand{\qna}[2]{
  {\normalsize #1}
  \begin{itemize}
  \item
    #2
  \end{itemize}
}

\renewcommand{\baselinestretch}{1.0}
